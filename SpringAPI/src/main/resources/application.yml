spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/angularproject
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.PostgresSQL82Dialect
      format_sql: true
    open-in-view: false